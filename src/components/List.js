import React from "react";

const List = (props) => {
  return (
    <div className="">
      <h5 className="card-title font-semibold capitalize ">{props.name}</h5>
    </div>
  ); 
};

export default List;
